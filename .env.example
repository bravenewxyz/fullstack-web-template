# ===========================================
# Self-Hosted Supabase Configuration
# ===========================================
#
# For local development with Docker:
# 1. Run: bun run docker:up
# 2. Wait for services to start (~30s)
# 3. Run: bun run db:push
# 4. Run: bun run dev
#
# See docker/.env.example for Docker-specific configuration
# ===========================================

# Supabase Configuration
# For self-hosted: http://localhost:8000
# For Supabase Cloud: https://your-project.supabase.co
VITE_SUPABASE_URL=http://localhost:8000

# Supabase Anon Key (public, safe for client-side)
# For self-hosted: copy from docker/.env after running setup
# For Supabase Cloud: get from Supabase dashboard
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (server-side only, keep secret!)
# For self-hosted: copy from docker/.env after running setup
# For Supabase Cloud: get from Supabase dashboard
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database URL (direct PostgreSQL connection for Drizzle ORM)
# For self-hosted: postgresql://postgres:your-password@localhost:5432/postgres
# For Supabase Cloud: get from Supabase dashboard > Settings > Database
DATABASE_URL=postgresql://postgres:postgres-dev-password-change-in-production@localhost:5432/postgres

# OpenRouter API (for LLM features)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key
# Model to use (see https://openrouter.ai/models for options)
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Google Maps API (optional, for map features)
# Get your API key from https://console.cloud.google.com/apis/credentials
# VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Optional
PORT=3000
NODE_ENV=development
