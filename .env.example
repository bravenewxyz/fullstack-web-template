# ===========================================
# Environment Configuration
# ===========================================
#
# DEPLOYMENT OPTIONS:
#
# 1. RAILWAY SELF-HOSTED (zero config)
#    - Just deploy to Railway, everything works automatically
#    - PostgreSQL + Supabase Auth run in the same container
#    - No environment variables needed!
#    - Add a volume at /data for persistent storage
#
# 2. RAILWAY WITH EXTERNAL POSTGRES (recommended for production)
#    - Add a Railway PostgreSQL service to your project
#    - Set DATABASE_URL to the Railway PostgreSQL connection string
#    - Supabase Auth will automatically use the external database
#    - No volume needed, data is in external PostgreSQL
#
# 3. LOCAL DEVELOPMENT with Docker:
#    - Run: bun run docker:up
#    - Wait for services to start (~30s)
#    - Run: bun run db:push
#    - Run: bun run dev
#
# 4. SUPABASE CLOUD:
#    - Create project at supabase.com
#    - Copy credentials below
#
# ===========================================

# Supabase Configuration (leave empty for self-hosted mode)
# For self-hosted Docker: http://localhost:8000
# For Supabase Cloud: https://your-project.supabase.co
VITE_SUPABASE_URL=

# Supabase Anon Key (public, safe for client-side)
# For self-hosted: auto-generated on Railway, or copy from docker/.env
# For Supabase Cloud: get from Supabase dashboard
VITE_SUPABASE_ANON_KEY=

# Supabase Service Role Key (server-side only, keep secret!)
# For self-hosted: auto-generated on Railway, or copy from docker/.env
# For Supabase Cloud: get from Supabase dashboard
SUPABASE_SERVICE_ROLE_KEY=

# Database URL (direct PostgreSQL connection for Drizzle ORM)
# For Railway with external Postgres: set to Railway PostgreSQL URL (disables self-contained Postgres)
# For Railway self-hosted: leave empty (auto-configured with self-contained Postgres)
# For self-hosted Docker: postgresql://postgres:your-password@localhost:5432/postgres
# For Supabase Cloud: get from Supabase dashboard > Settings > Database
DATABASE_URL=

# OpenRouter API (optional, for LLM features)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=
# Model to use (see https://openrouter.ai/models for options)
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Google Maps API (optional, for map features)
# Get your API key from https://console.cloud.google.com/apis/credentials
# VITE_GOOGLE_MAPS_API_KEY=

# Optional
PORT=3000
NODE_ENV=development
